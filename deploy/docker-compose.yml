version: '2.1'

services:
        mongo:
                image: mvertes/alpine-mongo
                expose: 
                        - 27017
                ports: 
                        - "27017:27017"
                hostname: mongo 
                stdin_open: true
                tty: true
                networks:
                        - elastest
                image: elastest/eim:0.1
                depends_on:
                        - mongo
                links:
                        - mongo
                stdin_open: true
                tty: true
                expose:
                        - 8080
                ports:
                        - "8081:8080" 
                networks:
                        - elastest

networks:
        elastest:
                driver: bridge
